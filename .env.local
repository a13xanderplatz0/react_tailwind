# Google OAuth Credentials
# Obtain these from Google Cloud Console: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID="YOUR_GOOGLE_CLIENT_ID"
GOOGLE_CLIENT_SECRET="YOUR_GOOGLE_CLIENT_SECRET"

# NextAuth Secret
# Generate a strong secret using: openssl rand -base64 32
# For development, any string works, but use a secure one for production.
AUTH_SECRET="YOUR_NEXTAUTH_SECRET"

# Required by NextAuth.js - should point to your deployed app URL in production
# For local development, it's usually http://localhost:PORT (e.g., http://localhost:9002)
AUTH_URL="http://localhost:9002"
AUTH_TRUST_HOST=true

# Optional: Google GenAI API Key (if using Genkit features)
# GOOGLE_GENAI_API_KEY="YOUR_GOOGLE_GENAI_API_KEY"
